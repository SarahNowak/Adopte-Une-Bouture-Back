<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}AdopteUneBouture{% endblock %}</title>
         <link rel="stylesheet" href="https://bootswatch.com/5/minty/bootstrap.min.css">
        <link rel="stylesheet" href="{{ asset('css/style.css') }}">
        <script src="https://kit.fontawesome.com/cbd2636078.js" crossorigin="anonymous"></script>
    </head>
    <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container-fluid">
            <a class="navbar-brand" href="{{path('admin_home')}}">AdopteUneBouture</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
            </button>
            {% if app.user  %}
            <div class="collapse navbar-collapse" id="navbarColor01">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('admin_user_browse')}}">Utilisateurs</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('admin_ads_browse')}}">Annonces</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('admin_messages_browse')}}">Messages</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('admin_plants_browse')}}">Fiches plantes</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('admin_category_browse')}}">Catégories</a>
                    </li>
                </ul>
                    <a href="{{ path('app_logout') }}"><button class="btn btn-danger">Déconnexion</button></a>
            {% endif %} 
            </div>
        </div>
    </nav>
        {% block body %}{% endblock %}
        </div>
    </body>
</html>
